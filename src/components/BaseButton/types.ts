const ROLES_ENUM = {
  BUTTON: 'button',
  SUBMIT: 'submit',
};

const TYPES_ENUM = {
  BG: 'bg',
  NOBG: 'nobg',
};

const BUTTON_TYPES = {
  ROLES_ENUM,
  TYPES_ENUM,
};

export { ROLES_ENUM, TYPES_ENUM, BUTTON_TYPES };
